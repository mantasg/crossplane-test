kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:

  # guestbook
  - containerPort: 80
    hostPort: 80
    protocol: TCP


  - containerPort: 30080
    hostPort: 9000
    protocol: TCP


  kubeadmConfigPatches:
  - |
    kind: InitConfiguration
    nodeRegistration:
      taints: []
- role: worker
- role: worker
- role: worker
